import React from 'react';
import './ContainerLoginRegistr.css';

const ContainerLoginRegistr = (props) => {
  return <div className='login-registr'>{props.children}</div>;
};

export default ContainerLoginRegistr;
